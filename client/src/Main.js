import React from 'react';
import styled from 'styled-components';

const Main = () => {
  return <Display>MAIN</Display>;
};

// Change name
const Display = styled.main`
  flex-grow: 2;
`;

export default Main;
